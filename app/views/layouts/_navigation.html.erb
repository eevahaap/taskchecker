<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%#= link_to 'Home', root_path, class: 'navbar-brand' %>
      <%= link_to 'Student Sign in', new_studentsession_path %>
      <%= link_to 'Teacher Sign in', new_teachersession_path  %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
          <% if not current_student.nil? %>

              <li><%= link_to 'Log out student', studentsession_path, method: :delete %></li>
              
              <% end %>
          
               <% if not current_teacher.nil? %>

              <li><%= link_to 'Log out teacher', teachersession_path, method: :delete %></li>
              
              <% end %>
  </div>
</nav>
